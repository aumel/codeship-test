machine:
  node:
    version: 0.10.33
  pre:
    - curl https://install.meteor.com | /bin/sh
    - meteor --version
    - curl -L http://git.io/1gisog | /bin/sh
    - ls -a
test:
  override:
    - node start_test.js
