machine:
  node:
    version: 0.10.33
  pre:
    - curl https://install.meteor.com | /bin/sh
    - meteor --version
    - curl -L http://git.io/1gisog | /bin/sh
    - ls -a
    - pwd
test:
  override:
    - node /home/ubuntu/start_test.js
